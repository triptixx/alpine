language: none

branches:
  only:
    - master

integrations:
  hub:
    - integrationName: DockerHub
      type: dockerRegistryLogin

env:
  matrix:
    - ALPINE_TAG: '3.7'
    - ALPINE_TAG: '3.8'
    - ALPINE_TAG: '3.9'
    - ALPINE_TAG: '3.10'
#    - TEST=1 FOO=foo

build:
#  post_ci:
#    - docker build --build-arg ALPINE_TAG=${ALPINE_TAG} -t ${ORG_NAME}/my_image:${ALPINE_TAG} .
  ci:
    - 'echo "building image ${ALPINE_TAG} ${ORG_NAME}/${REPO_NAME}"'
